apply plugin: 'war'

dependencies {
    implementation(
            'javax.servlet:jstl',
            'org.apache.tomcat.embed:tomcat-embed-jasper',
            'javax.servlet:javax.servlet-api'
    )
}

war {
    archivesBaseName = 'jsp'
}
