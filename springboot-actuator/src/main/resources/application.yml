server:
  port: 8081

spring:
  application:
    name: springboot-actuator
  boot:
    admin:
      client:
        url: http://localhost:8088
        instance:
          prefer-ip: true
info:
  name: actuator

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

logging:
  file: /log/actuator.log