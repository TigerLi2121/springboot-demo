<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mm.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.mm.entity.UserEntity">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_name" property="userName" jdbcType="VARCHAR"/>
        <result column="pass_word" property="passWord" jdbcType="VARCHAR"/>
        <result column="user_sex" property="userSex" javaType="com.mm.enums.UserSexEnum"/>
        <result column="nick_name" property="nickName" jdbcType="VARCHAR"/>
        <result column="gmt_create" property="gmtCreate" jdbcType="DATE"/>
        <result column="gmt_modified" property="gmtModified" jdbcType="DATE"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_name, pass_word, user_sex, nick_name, gmt_create, gmt_modified
    </sql>

    <select id="getAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM users
    </select>

    <select id="getOne" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM users
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.mm.entity.UserEntity">
        INSERT INTO
        users
        (user_name,pass_word,user_sex,gmt_create,gmt_modified)
        VALUES
        (#{userName}, #{passWord}, #{userSex}, #{gmtCreate},#{gmtModified})
    </insert>

    <update id="update" parameterType="com.mm.entity.UserEntity">
        UPDATE
        users
        SET
        <trim suffixOverrides=",">
            <if test="userName != null">user_name = #{userName},</if>
            <if test="passWord != null">pass_word = #{passWord},</if>
            <if test="gmtModified != null">gmt_modified = #{gmtModified},</if>
            <if test="nickName != null">nick_name = #{nickName},</if>
        </trim>
        WHERE
        id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM
        users
        WHERE
        id =#{id}
    </delete>

</mapper>